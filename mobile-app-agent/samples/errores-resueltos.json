[
    {
        "input": "TS2528: A module cannot have multiple default exports.\nArchivo: HomeScreen.tsx",
        "output": "Se eliminó la línea duplicada 'export default HomeScreen;' al final del archivo."
    },
    {
        "input": "TS2322: Type '{ uri: string | { uri: string; }; }' is not assignable to type 'ImageSourcePropType'.",
        "output": "Se reemplazó por: 'source={{ uri: typeof image === 'string' ? image : image.uri }}' y se ajustó el tipo."
    },
    {
        "input": "TS7053: Element implicitly has an 'any' type because expression of type 'string' can't be used to index type '{}'",
        "output": "Se tipó correctamente el objeto con Record<string, any> para evitar el error implícito."
    },
    {
        "input": "Error de importación: import Amplify from 'aws-amplify'",
        "output": "Se reemplazó por: import * as Amplify from 'aws-amplify'"
    }
]