service: boat-rental-backend

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  stage: ${opt:stage, "dev"}
  environment:
    STAGE: ${self:provider.stage}

functions:
  hello:
    handler: src/index.handler
    events:
      - http:
          path: hello
          method: get
          cors: true

resources:
  Resources:
    # Puedes agregar recursos de DynamoDB aquí más tarde
    
# NO PLUGINS POR AHORA - están causando conflictos
